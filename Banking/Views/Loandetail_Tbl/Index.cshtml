@model IEnumerable<Banking.Models.Loandetail_Tbl>

@{
    ViewBag.Title = "Index";
}

<h2>Apply for Loans</h2>

<p>
    @Html.ActionLink("Apply for Loan", "Create")
</p>
@*<table class="table table-bordered table-hover">
    <tr>
        <th class="bg-info">
            Loan Type
        </th>
        <th class="bg-info">
            Amount
        </th>
        <th class="bg-info">
            Interest Rate
        </th>
        <th class="bg-info">
            Duration(in months)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Loan_Tbl.type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loan_Tbl.amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loan_Tbl.interest)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loan_Tbl.duration)
            </td>
        </tr>
    }

</table>*@

<label> @ViewBag.data</label>





@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.accountno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Loan_Tbl.type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.accountno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Loan_Tbl.type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>*@
